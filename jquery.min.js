$(document).ready(function() 
{

$('#user-contact').on('input', function() 
{
	  $cf = $('#user-contact');
    $cf.blur(function(e){
        phone = $(this).val();
        phone = phone.replace(/[^0-9]/g,'');
        if (phone.length != 10)
        {
            alert('Phone number must be of 10 digits.');
            $('#phonenumber').val('');
            $('#phonenumber').focus();
        }
    });
$('#user-password').on('input', function() 
{
    required: true,
    minlength: 6,
    maxlength: 10

});
$('#user-password2').on('input', function() 
{
	required: true,
    equalTo: "#user_password",
    minlength: 6,
    maxlength: 10
});
}
